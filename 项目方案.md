# 数字炸弹小游戏 · 项目方案

> 目标：Python 实现的多人在线「数字炸弹」网页游戏后端，配套一个可直接跑通的单页前端。
> 本文档为首轮开发前的设计约定，确认后即可进入编码。

---

## 一、需求拆解与规则确认

### 1.1 你提出的原始需求

| 编号 | 需求 | 实现要点 |
|---|---|---|
| 1 | 主界面 → 加入大厅（需填微信昵称）→ 大厅内可「开始游戏」/「关闭大厅」；游戏进行中拒绝新玩家加入；关闭大厅则全员退回主界面 | 四态状态机 + 服务端主动广播 |
| 2 | 开始游戏时统计人数：≤4 人范围 0–100，>4 人范围 0–1000；按加入先后顺序轮流 | 人数在「点击开始」那一刻快照锁定 |
| 3 | 轮到某玩家时前端显示「请输入从 x - y」+ 输入框；越界要求重输；合法输入后判定并收缩边界；命中炸弹则显示「昵称，Boom！」，3 秒后回大厅 | 非法输入不消耗回合；Boom 后回大厅而非主界面 |
| 4 | 首轮开发完成后输出 README，含游戏规则与部署说明 | 见第七节 |

### 1.2 关键规则设计（默认约定，可调整）

**边界与炸弹的取值方式 —— 采用开区间 + 严格收缩**

```
初始：left = 0, right = N (100 或 1000)
炸弹：bomb = random.randint(left+1, right-1)   # 开区间内随机整数
玩家可猜范围：(left, right) 开区间，即 left+1 到 right-1

判定：
  guess == bomb  → Boom，本局结束
  guess <  bomb  → left  = guess
  guess >  bomb  → right = guess
```

这样设计的理由：
- 每次合法猜测都让区间**至少缩小 1**，不会死循环，游戏必然在有限回合内结束；
- 炸弹数字**永远严格落在 `(left, right)` 内**（即 `left < bomb < right`），前端提示的「请输入从 x+1 到 y-1」与实际可猜范围完全一致，不会出现「明明按提示输入却被告知非法」；
- 边界值 `left` 和 `right` 代表已确认安全的范围，**不可作为猜测**。

**其他规则细节**

| 场景 | 默认处理 |
|---|---|
| 人数下限 | 1 人即可开始（自己玩） |
| 人数上限 | 不设硬上限（如需限制请说明） |
| 人数统计时机 | 点击「开始游戏」瞬间快照；游戏开始后的断线不再改变范围 |
| 昵称规则 | 去首尾空格，非空，长度 ≤ 20 字符 |
| 昵称重复 | **允许**（微信昵称本就可能重名），内部用 `playerId` 区分，界面重名时显示为「昵称(2)」 |
| 非法输入 | 非整数 / 越界 / 空值 → 返回错误提示，**不推进回合**，同一玩家继续输入 |
| 非当前玩家提交 | 拒绝并提示「还没轮到你」 |
| 游戏中有人断线 | 从玩家列表移除并重算轮次索引；若断线者正是当前回合玩家，自动跳到下一位 |
| 全员断线 | 房间回到 `IDLE`（主界面）状态 |
| Boom 之后 | 3 秒后回**大厅**（保留玩家列表），边界与炸弹重置，从第 1 位玩家重新开始 |
| 游戏中关闭房间 | 允许任意玩家触发，直接全员退回主界面（异常兜底，避免卡死） |

> ⚠️ 待确认点 B：昵称是否要求唯一？我默认允许重复。
> ⚠️ 待确认点 C：游戏进行中是否保留「关闭」能力？我默认保留作为兜底。

---

## 二、技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| Web 框架 | **FastAPI** | 原生 async + WebSocket 支持，自带 Pydantic 校验与 OpenAPI 文档 |
| 服务器 | **uvicorn** | ASGI 标准实现 |
| 实时通信 | **WebSocket**（单端点 `/ws`） | 见下方说明 |
| 状态存储 | **进程内存** | 游戏状态天然短暂，无需数据库 |
| 前端 | **单页原生 HTML/CSS/JS** | 零构建、零依赖，打开即用，便于端到端验证后端逻辑 |
| 测试 | **pytest + httpx/pytest-asyncio** | 覆盖纯逻辑单测与 WebSocket 端到端流程 |

**为什么必须用 WebSocket 而不是 HTTP 轮询**

你的需求里有 4 处「服务端驱动的全员状态变化」，客户端无法自行感知：
1. 任意玩家点「开始游戏」→ 所有人跳到游戏界面；
2. 任意玩家点「关闭大厅」→ 所有人退回主界面；
3. 回合轮转 → 只有当前玩家显示输入框，其他人显示等待；
4. Boom → 所有人看到提示，3 秒后统一回大厅。

轮询虽可实现，但延迟高（要么卡顿要么请求量大）、且需要额外维护版本号做增量比对。WebSocket 一次推送即可让所有客户端状态一致。

> ⚠️ 待确认点 D：项目名为 `number-bomb-game-Server`，但需求 3 明确描述了前端显示。我默认**后端为主 + 内置一个可用的简易前端**（能完整跑通全流程，但不是精美 UI）。若你已有独立前端、只需要 API，请告知，我会改为「后端 + WebSocket 协议文档」交付。

---

## 三、服务端状态机

全局单房间（同一时刻只存在一个大厅 / 一局游戏），符合「当下没有游戏进行时」的描述。

```
                  join_lobby(nickname)
   ┌────────┐ ─────────────────────────► ┌────────┐
   │  IDLE  │                            │ LOBBY  │◄──────┐
   │ 主界面 │ ◄───────────────────────── │  大厅  │       │
   └────────┘      close_lobby()         └───┬────┘       │
                                             │ start_game()│ 3 秒后
                                             ▼             │ (重置边界/炸弹/轮次
                                        ┌─────────┐        │  保留玩家列表)
                                        │ PLAYING │        │
                                        │ 游戏中  │────────┤
                                        └────┬────┘        │
                          guess() 命中炸弹   │             │
                                             ▼             │
                                        ┌─────────┐        │
                                        │  BOOM   │ ───────┘
                                        └─────────┘

  任意状态下 close_lobby() / 全员断线  →  IDLE
  PLAYING 下 join_lobby()             →  拒绝「游戏进行中，暂不能加入」
```

**PLAYING 内部子状态**：`currentPlayerIndex` 指向玩家列表中的当前回合玩家；每次合法且未命中的猜测后 `index = (index + 1) % len(players)`。

---

## 四、通信协议

### 4.1 WebSocket 端点

`ws://<host>:<port>/ws` — 连接即建立会话，服务端为该连接分配 `playerId`（仅在加入大厅后生效）。

### 4.2 客户端 → 服务端

```jsonc
{ "action": "join_lobby",  "nickname": "小明" }
{ "action": "start_game" }
{ "action": "close_lobby" }
{ "action": "guess",       "value": 50 }
{ "action": "ping" }
```

### 4.3 服务端 → 客户端

**状态广播**（每次状态变化推给全部连接，前端按 `phase` 切换界面）

```jsonc
{
  "type": "state",
  "phase": "idle" | "lobby" | "playing" | "boom",
  "you": { "id": "p_3f2a", "nickname": "小明", "inRoom": true },
  "room": {
    "players": [
      { "id": "p_3f2a", "nickname": "小明", "online": true, "isYou": true },
      { "id": "p_9b1c", "nickname": "小红", "online": true, "isYou": false }
    ],
    "hostId": "p_3f2a"          // 首个加入者，前端可给个「房主」标记
  },
  "game": {                      // phase = playing / boom 时有值
    "left": 0,
    "right": 100,
    "playerCount": 2,
    "currentPlayerId": "p_9b1c",
    "currentPlayerNickname": "小红",
    "isYourTurn": false,
    "lastGuess": { "nickname": "小明", "value": 50, "result": "safe" },
    "boomNickname": null         // phase = boom 时为踩雷玩家昵称
  },
  "serverTime": 1760000000000
}
```

**定向错误消息**（只发给操作者，不广播，避免污染全局状态）

```jsonc
{ "type": "error", "code": "OUT_OF_RANGE", "message": "请输入 0 - 100 之间的整数" }
```

错误码约定：`INVALID_NICKNAME` / `GAME_IN_PROGRESS` / `NOT_IN_ROOM` / `NOT_YOUR_TURN` / `OUT_OF_RANGE` / `INVALID_NUMBER` / `INVALID_PHASE`。

**Boom 提示消息**（用于前端做 3 秒倒计时）

```jsonc
{ "type": "boom", "nickname": "小红", "bomb": 42, "backToLobbyInMs": 3000 }
```

> 说明：炸弹数字只在 Boom 时公布，游戏过程中绝不下发（否则前端可作弊）。

### 4.4 辅助 HTTP 端点

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/` | 前端单页 |
| GET | `/api/health` | 存活检查（返回 `{status, phase, playerCount}`） |
| GET | `/api/state` | 只读状态快照，调试用 |
| GET | `/docs` | FastAPI 自动生成的接口文档 |

---

## 五、项目结构

```
number-bomb-game-Server/
├── app/
│   ├── __init__.py
│   ├── main.py          # FastAPI 实例、路由与静态目录挂载、生命周期钩子
│   ├── config.py        # 可调参数集中管理（见下）
│   ├── models.py        # Pydantic 消息模型 + Phase / ErrorCode 枚举
│   ├── game.py          # ★ 纯游戏逻辑 GameRoom，不含任何 IO，可独立单测
│   └── ws.py            # ConnectionManager（连接管理/广播）+ 消息分发路由
├── static/
│   ├── index.html       # 单页：主界面 / 大厅 / 游戏 / Boom 四态切换
│   ├── style.css
│   └── app.js           # WebSocket 客户端 + 渲染 + 断线自动重连
├── tests/
│   ├── test_game.py     # 规则单测（范围选择、边界收缩、轮次、非法输入）
│   └── test_ws.py       # WebSocket 端到端流程测试
├── requirements.txt
├── run.py               # 开发启动入口
├── Dockerfile           # 可选，容器化部署
└── README.md            # 游戏规则 + 部署说明（首轮开发完成后产出）
```

**`config.py` 集中管理的参数**

```python
SMALL_ROOM_MAX_PLAYERS = 4        # 人数阈值
SMALL_ROOM_RANGE       = (0, 100)
LARGE_ROOM_RANGE       = (0, 1000)
BOOM_DURATION_MS       = 3000     # Boom 界面停留时长
NICKNAME_MAX_LEN       = 20
MAX_PLAYERS            = None     # None 表示不限
ALLOW_DUPLICATE_NICK   = True
```

**设计原则：`game.py` 与 `ws.py` 严格分离。** 所有规则计算（范围选择、边界收缩、轮次推进、合法性校验）都是纯函数式的状态转移，不碰网络；`ws.py` 只负责收发消息、维护连接、调用 `game.py` 并广播结果。这样规则可以用 pytest 直接覆盖，无需起服务。

---

## 六、开发计划

| 阶段 | 内容 | 产出 |
|---|---|---|
| **P0 环境** | 安装依赖（fastapi / uvicorn / pydantic / pytest），验证 Python 3.14 兼容性 | `requirements.txt` 可复现 |
| **P1 游戏内核** | `config.py`、`models.py`、`game.py` 状态机 + 全部规则单测通过 | 纯逻辑可测 |
| **P2 通信层** | `ws.py` 连接管理、消息路由、广播、断线处理、Boom 定时器 | WebSocket 服务可连 |
| **P3 前端** | 单页四态界面、输入校验、断线重连 | 浏览器可完整玩一局 |
| **P4 端到端验证** | 多标签页并发测试：开始/关闭/越界/命中/断线/轮次 | 全流程通过 |
| **P5 文档** | `README.md`（游戏规则 + 部署说明）、可选 Dockerfile | 交付完成 |

**P4 必须验证的场景清单**
- 4 人开局 → 范围 0–100；5 人开局 → 范围 0–1000
- 边界严格收缩，炸弹始终在 `(left, right)` 内（开区间），游戏必然结束
- 越界 / 非数字 / 非当前玩家提交 → 均不推进回合
- 游戏进行中点「加入大厅」→ 被拒绝并提示
- 点「关闭大厅」→ 全员退回主界面
- 命中炸弹 → 全员看到「昵称，Boom！」→ 3 秒后回大厅，玩家列表保留
- 游戏中玩家断线 → 轮次正确跳过；全员断线 → 房间归零

---

## 七、README 交付内容（P5）

1. **项目简介** — 一句话说明与技术栈
2. **游戏规则** — 完整规则说明（人数与范围对照表、轮次规则、边界收缩规则、Boom 流程、异常处理）
3. **快速开始** — 环境要求、安装依赖、启动命令、访问地址
4. **部署说明**
   - 本地开发：`pip install -r requirements.txt` → `python run.py`
   - 生产环境：`uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1`
   - Nginx 反向代理：**必须配置 WebSocket Upgrade 头**（附完整配置片段）
   - Docker 部署：`Dockerfile` + 运行命令
5. **WebSocket 协议文档** — 消息格式与错误码表（便于对接独立前端）
6. **配置项说明** — `config.py` 各参数含义
7. **重要限制说明**
   > ⚠️ 游戏状态存于进程内存，**只能单 worker 运行**。多 worker 会导致玩家被分散到不同进程、互相看不见。若需横向扩展或多实例部署，必须引入 Redis 做共享状态 + 发布订阅，这属于二期改造范围。
8. **测试运行方式**

---

## 八、待你确认的 4 个点

| 编号 | 问题 | 我的默认方案 |
|---|---|---|
| A | 猜边界值算合法吗？ | 不合法（开区间 `(left, right)`，边界值代表已确认安全的范围，不可猜测） |
| B | 昵称允许重复吗？ | 允许，内部用 playerId 区分，重名界面显示序号 |
| C | 游戏进行中保留「关闭房间」吗？ | 保留，作为卡死兜底 |
| D | 需要我一并做前端页面吗？ | 需要，内置一个可用但朴素的单页，保证端到端可验证 |

确认后我按 P0 → P5 顺序推进；如无异议，我直接按上述默认方案开工。
